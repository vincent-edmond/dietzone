# Plan 1 — Fondation (scaffolding) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
> **Lire d'abord :** `2026-06-02-00-conventions.md` (contrat technique partagé).

**Goal:** Mettre en place une application Next.js+TypeScript+Tailwind+shadcn fonctionnelle, connectée à Supabase, avec tests (Vitest) et qualité (lint/typecheck) verts, prête à recevoir le catalogue.

**Architecture:** App Router Next.js 15 en structure `src/`. Clients Supabase (browser/server) isolés dans `lib/supabase`. Helpers money testés en TDD. Layout global + page d'accueil placeholder. CI locale via scripts npm.

**Tech Stack:** Next.js 15, TypeScript, Tailwind, shadcn/ui, Supabase (`@supabase/ssr`), Vitest, ESLint/Prettier.

---

### Task 1 : Initialiser le projet Next.js

**Files:**
- Create: tout le squelette Next.js à la racine du repo.

- [ ] **Step 1: Scaffolder Next.js dans le repo existant**

Le repo contient déjà `.git`, `docs/`, `.gitignore`. Scaffolder dans un dossier temporaire puis fusionner pour ne pas écraser `docs/`.

Run :
```bash
npx create-next-app@latest dietzone-app --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm --no-turbopack
rsync -a --exclude='.git' --exclude='.gitignore' dietzone-app/ ./
rm -rf dietzone-app
```
Expected: présence de `src/app/page.tsx`, `package.json`, `tailwind.config.ts`, `tsconfig.json`.

- [ ] **Step 2: Vérifier que l'app démarre**

Run: `npm run dev` puis ouvrir http://localhost:3000
Expected: page Next.js par défaut s'affiche. Arrêter avec Ctrl+C.

- [ ] **Step 3: Commit**

```bash
git add -A
git commit -m "chore: scaffold Next.js app (TS, Tailwind, App Router, src/)"
```

---

### Task 2 : Mettre en place Vitest (TDD)

**Files:**
- Create: `vitest.config.ts`, `tests/unit/sanity.test.ts`
- Modify: `package.json` (scripts)

- [ ] **Step 1: Installer les dépendances de test**

Run:
```bash
npm i -D vitest @testing-library/react @testing-library/jest-dom jsdom @vitejs/plugin-react
```

- [ ] **Step 2: Écrire la config Vitest**

Create `vitest.config.ts`:
```typescript
import { defineConfig } from 'vitest/config'
import react from '@vitejs/plugin-react'
import path from 'path'

export default defineConfig({
  plugins: [react()],
  test: { environment: 'jsdom', globals: true, setupFiles: ['./tests/setup.ts'] },
  resolve: { alias: { '@': path.resolve(__dirname, './src') } },
})
```

Create `tests/setup.ts`:
```typescript
import '@testing-library/jest-dom/vitest'
```

- [ ] **Step 3: Écrire un test sanity (rouge d'abord)**

Create `tests/unit/sanity.test.ts`:
```typescript
import { describe, it, expect } from 'vitest'
describe('sanity', () => {
  it('runs the test runner', () => {
    expect(1 + 1).toBe(2)
  })
})
```

- [ ] **Step 4: Ajouter les scripts npm**

Modify `package.json` scripts:
```json
"scripts": {
  "dev": "next dev",
  "build": "next build",
  "start": "next start",
  "lint": "next lint",
  "typecheck": "tsc --noEmit",
  "test": "vitest run",
  "test:watch": "vitest"
}
```

- [ ] **Step 5: Lancer les tests**

Run: `npm test`
Expected: 1 test PASS.

- [ ] **Step 6: Typecheck + lint**

Run: `npm run typecheck && npm run lint`
Expected: aucun erreur.

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "test: configure Vitest + testing-library"
```

---

### Task 3 : Helpers money (TDD complet)

**Files:**
- Create: `src/lib/money.ts`
- Test: `tests/unit/money.test.ts`

- [ ] **Step 1: Écrire les tests (rouge)**

Create `tests/unit/money.test.ts`:
```typescript
import { describe, it, expect } from 'vitest'
import { formatEuros, eurosToCents, applyProDiscount } from '@/lib/money'

describe('formatEuros', () => {
  it('formate des centimes en euros FR', () => {
    expect(formatEuros(6500)).toBe('65,00 €')
    expect(formatEuros(9900)).toBe('99,00 €')
    expect(formatEuros(0)).toBe('0,00 €')
  })
})
describe('eurosToCents', () => {
  it('convertit des euros en centimes entiers', () => {
    expect(eurosToCents(65)).toBe(6500)
    expect(eurosToCents(65.9)).toBe(6590)
    expect(eurosToCents(0.1)).toBe(10)
  })
})
describe('applyProDiscount', () => {
  it('applique une remise % et arrondit au centime', () => {
    expect(applyProDiscount(10000, 20)).toBe(8000)
    expect(applyProDiscount(9999, 15)).toBe(8499) // round(9999*0.85)=8499.15->8499
    expect(applyProDiscount(6500, 0)).toBe(6500)
  })
})
```

- [ ] **Step 2: Vérifier l'échec**

Run: `npm test -- money`
Expected: FAIL (module non trouvé).

- [ ] **Step 3: Implémenter**

Create `src/lib/money.ts`:
```typescript
const fmt = new Intl.NumberFormat('fr-FR', { minimumFractionDigits: 2, maximumFractionDigits: 2 })

export function formatEuros(cents: number): string {
  return `${fmt.format(cents / 100)} €`
}
export function eurosToCents(euros: number): number {
  return Math.round(euros * 100)
}
export function applyProDiscount(cents: number, percent: number): number {
  return Math.round((cents * (100 - percent)) / 100)
}
```

- [ ] **Step 4: Tests verts**

Run: `npm test -- money`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat: money helpers (cents <-> euros, pro discount)"
```

---

### Task 4 : Types de domaine

**Files:**
- Create: `src/types/domain.ts`

- [ ] **Step 1: Créer les types** (copie depuis le contrat `00-conventions.md`, section « Types de domaine »)

Create `src/types/domain.ts` avec exactement le bloc TypeScript de `00-conventions.md` (`Role`, `OrderStatus`, `Fulfillment`, `PaymentMethod`, `ProStatus`, `Money`, `CartLine`).

- [ ] **Step 2: Typecheck**

Run: `npm run typecheck`
Expected: aucune erreur.

- [ ] **Step 3: Commit**

```bash
git add -A && git commit -m "feat: domain types"
```

---

### Task 5 : Clients Supabase

**Files:**
- Create: `src/lib/supabase/client.ts` (browser), `src/lib/supabase/server.ts` (RSC/route), `src/lib/supabase/admin.ts` (service role)
- Test: `tests/unit/supabase-env.test.ts`

- [ ] **Step 1: Installer les libs**

Run: `npm i @supabase/supabase-js @supabase/ssr`

- [ ] **Step 2: Test garde-fou d'environnement (rouge)**

Create `tests/unit/supabase-env.test.ts`:
```typescript
import { describe, it, expect } from 'vitest'
import { requireEnv } from '@/lib/supabase/env'

describe('requireEnv', () => {
  it('renvoie la valeur si présente', () => {
    process.env.TEST_X = 'abc'
    expect(requireEnv('TEST_X')).toBe('abc')
  })
  it('jette si absente', () => {
    delete process.env.TEST_Y
    expect(() => requireEnv('TEST_Y')).toThrow(/TEST_Y/)
  })
})
```

- [ ] **Step 3: Implémenter `env.ts`**

Create `src/lib/supabase/env.ts`:
```typescript
export function requireEnv(name: string): string {
  const v = process.env[name]
  if (!v) throw new Error(`Variable d'environnement manquante : ${name}`)
  return v
}
```

- [ ] **Step 4: Test vert**

Run: `npm test -- supabase-env`
Expected: PASS.

- [ ] **Step 5: Implémenter les clients**

Create `src/lib/supabase/client.ts`:
```typescript
import { createBrowserClient } from '@supabase/ssr'
export function createClient() {
  return createBrowserClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
  )
}
```

Create `src/lib/supabase/server.ts`:
```typescript
import { createServerClient } from '@supabase/ssr'
import { cookies } from 'next/headers'

export async function createClient() {
  const cookieStore = await cookies()
  return createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll: () => cookieStore.getAll(),
        setAll: (toSet) => toSet.forEach(({ name, value, options }) => cookieStore.set(name, value, options)),
      },
    },
  )
}
```

Create `src/lib/supabase/admin.ts`:
```typescript
import { createClient as createSb } from '@supabase/supabase-js'
import { requireEnv } from './env'
// Service role : SERVEUR UNIQUEMENT. Ne jamais importer côté client.
export function createAdminClient() {
  return createSb(requireEnv('NEXT_PUBLIC_SUPABASE_URL'), requireEnv('SUPABASE_SERVICE_ROLE_KEY'), {
    auth: { persistSession: false },
  })
}
```

- [ ] **Step 6: Typecheck + commit**

```bash
npm run typecheck
git add -A && git commit -m "feat: supabase clients (browser, server, admin)"
```

---

### Task 6 : `.env.example` + documentation env

**Files:**
- Create: `.env.example`

- [ ] **Step 1: Créer `.env.example`** (mêmes clés que `00-conventions.md`, valeurs vides)

Create `.env.example` avec les clés listées dans le contrat (sans valeurs). `.env*.local` est déjà gitignoré (cf `.gitignore` racine).

- [ ] **Step 2: Commit**

```bash
git add -A && git commit -m "chore: .env.example"
```

> ⚠️ **Action humaine requise** : créer un projet Supabase, un compte Stripe (mode test) et un compte Resend ; remplir `.env.local` avec les vraies clés avant le Plan 2.

---

### Task 7 : Initialiser shadcn/ui + layout global

**Files:**
- Modify: `src/app/layout.tsx`, `src/app/globals.css`
- Create: `src/components/ui/*` (via shadcn), `src/app/page.tsx` (placeholder)

- [ ] **Step 1: Initialiser shadcn**

Run:
```bash
npx shadcn@latest init -d
npx shadcn@latest add button
```
Expected: `components.json` créé, `src/components/ui/button.tsx` présent.

- [ ] **Step 2: Layout global avec métadonnées**

Replace `src/app/layout.tsx`:
```tsx
import type { Metadata } from 'next'
import './globals.css'

export const metadata: Metadata = {
  title: { default: 'DietZone — Nutrition sportive à La Réunion', template: '%s | DietZone' },
  description: 'Boutique de nutrition sportive à St-Denis (974). Protéines, créatine, pre-workout. Livraison sur toute l’île et retrait magasin.',
}

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="fr">
      <body className="min-h-screen bg-white text-neutral-900 antialiased">{children}</body>
    </html>
  )
}
```

- [ ] **Step 3: Page d'accueil placeholder**

Replace `src/app/page.tsx`:
```tsx
import { Button } from '@/components/ui/button'

export default function Home() {
  return (
    <main className="mx-auto max-w-3xl px-4 py-24 text-center">
      <h1 className="text-4xl font-bold tracking-tight">DietZone</h1>
      <p className="mt-4 text-neutral-600">Expert en nutrition sportive — St-Denis, La Réunion.</p>
      <Button className="mt-8">Découvrir la boutique</Button>
    </main>
  )
}
```

- [ ] **Step 4: Vérifier build + lint + typecheck**

Run: `npm run build && npm run typecheck && npm run lint`
Expected: build réussi, aucune erreur.

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat: shadcn init + layout global + accueil placeholder"
```

---

### Task 8 : Test composant (smoke) + README

**Files:**
- Create: `tests/component/home.test.tsx`, `README.md`

- [ ] **Step 1: Test composant (rouge)**

Create `tests/component/home.test.tsx`:
```tsx
import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'
import Home from '@/app/page'

describe('Home', () => {
  it('affiche le nom de la marque', () => {
    render(<Home />)
    expect(screen.getByRole('heading', { name: 'DietZone' })).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: Test vert**

Run: `npm test`
Expected: tous les tests PASS.

- [ ] **Step 3: README minimal**

Create `README.md` : nom du projet, stack, comment lancer (`npm install`, copier `.env.example` → `.env.local`, `npm run dev`), scripts (`test`, `lint`, `typecheck`, `build`).

- [ ] **Step 4: Commit**

```bash
git add -A && git commit -m "test: home smoke test + docs: README"
```

---

## Self-Review (Plan 1)

- **Couverture spec §7** : Next.js ✓, Vercel-ready ✓, Tailwind+shadcn ✓, Supabase clients ✓, RLS (déléguée Plan 2 où le schéma existe) ✓, Stripe (Plan 3) ✓.
- **Pas de placeholder de plan** : tout le code est fourni.
- **Cohérence des types** : `money.ts` et `domain.ts` figés ici, réutilisés tels quels dans les plans suivants.

**Sortie de Plan 1 :** app qui build/test/lint vert, clients Supabase prêts, helpers money testés. ➡️ Plan 2.
