# Plan 4 — Comptes & Espace PRO — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development ou superpowers:executing-plans.
> **Pré-requis :** Plans 1-3 terminés. Lire `00-conventions.md`.

**Goal:** Authentification client (optionnelle pour acheter), espace « Mon compte » (profil + commandes), et espace PRO : demande en ligne → validation par Alexandre → rôle `pro` → prix pro (remise globale %) appliqués partout.

**Architecture:** Supabase Auth (email/mot de passe) via `@supabase/ssr` + middleware Next.js pour rafraîchir la session. Table `profiles` (1-1 avec `auth.users`) portant le rôle. Le statut pro est lu côté serveur et injecté dans le calcul des totaux (`computeTotals(... isPro ...)`).

**Tech Stack:** Supabase Auth, Next.js middleware, RLS.

---

### Task 1 : Schéma profiles + pro_applications + trigger

**Files:**
- Create: `supabase/migrations/0006_profiles.sql`

- [ ] **Step 1: Migration**

Create `supabase/migrations/0006_profiles.sql`:
```sql
create table profiles (
  id uuid primary key references auth.users(id) on delete cascade,
  email text not null,
  full_name text,
  phone text,
  role text not null default 'customer' check (role in ('customer','pro','admin')),
  created_at timestamptz not null default now()
);
create table pro_applications (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references auth.users(id) on delete cascade,
  company_name text not null,
  siret text,
  phone text,
  message text,
  status text not null default 'pending' check (status in ('pending','approved','rejected')),
  created_at timestamptz not null default now(),
  reviewed_at timestamptz
);
-- Crée automatiquement un profile à l'inscription
create function handle_new_user() returns trigger language plpgsql security definer as $$
begin
  insert into profiles (id, email) values (new.id, new.email);
  return new;
end; $$;
create trigger on_auth_user_created after insert on auth.users
  for each row execute function handle_new_user();

alter table profiles enable row level security;
alter table pro_applications enable row level security;
create policy "user reads own profile" on profiles for select using (auth.uid() = id);
create policy "user updates own profile" on profiles for update using (auth.uid() = id);
create policy "user reads own applications" on pro_applications for select using (auth.uid() = user_id);
create policy "user creates own application" on pro_applications for insert with check (auth.uid() = user_id);
-- Admin gère tout : policies admin ajoutées au Plan 5.
```

- [ ] **Step 2:** Appliquer, `get_advisors` security OK. **Step 3:** commit `feat(db): profiles + pro_applications + new-user trigger + RLS`.

---

### Task 2 : Middleware session + helpers auth serveur

**Files:**
- Create: `src/middleware.ts`, `src/features/account/auth.ts`
- Test: `tests/unit/auth-helpers.test.ts`

- [ ] **Step 1:** `middleware.ts` rafraîchit la session Supabase (`@supabase/ssr` `updateSession`) sur toutes les routes sauf assets.
- [ ] **Step 2: Helpers serveur** `auth.ts` : `getCurrentUser()` (user + profile), `getRole()`, `requireRole(role)` (redirige si insuffisant). 
- [ ] **Step 3:** Test pur `isProRole`/`canAccessPro` (fonctions pures testables sans réseau). **Step 4:** test vert. **Step 5:** commit `feat(account): session middleware + server auth helpers`.

---

### Task 3 : Pages connexion / inscription

**Files:**
- Create: `src/app/(account)/connexion/page.tsx`, `src/app/(account)/inscription/page.tsx`, `src/features/account/actions.ts`

- [ ] **Step 1:** Server Actions `signIn`, `signUp`, `signOut` (`'use server'`) via client serveur Supabase. **Step 2:** Pages avec formulaires shadcn (`Input`, `Button`, `Label`), gestion d'erreur affichée. **Step 3:** Test composant : le formulaire de connexion rend email+mot de passe+submit. **Step 4:** Vérif manuelle : inscription crée un profile (trigger), connexion ouvre la session. **Step 5:** commit `feat(account): sign in / sign up / sign out`.

---

### Task 4 : Espace « Mon compte » (profil + commandes)

**Files:**
- Create: `src/app/(account)/compte/page.tsx`, `src/app/(account)/compte/commandes/page.tsx`, `src/features/account/queries.ts`

- [ ] **Step 1:** `compte/page.tsx` (protégé via `requireRole`) : affiche/édite profil (full_name, phone) → Server Action update. **Step 2:** `queries.ts` `listMyOrders()` (RLS filtre par `auth.uid()`). **Step 3:** `compte/commandes/page.tsx` : liste des commandes du client (date, total, statut). **Step 4:** Test transform `toOrderSummary`. **Step 5:** commit `feat(account): my account profile + orders`.

---

### Task 5 : Lier les commandes invité au compte créé

**Files:**
- Modify: `src/app/(shop)/commande/[id]/page.tsx`, `src/features/account/actions.ts`

- [ ] **Step 1:** Sur `/commande/[id]`, la proposition « créer un compte » (Plan 3) appelle `signUp` puis une action `claimOrder(orderId)` qui, si l'email du compte = email de la commande invité, met `orders.user_id = auth.uid()` (via service role, vérif email). **Step 2:** Test de la règle `emailsMatch(orderEmail, userEmail)` (insensible à la casse). **Step 3:** commit `feat(account): claim guest order on signup`.

---

### Task 6 : Landing PRO + formulaire de demande

**Files:**
- Create: `src/app/pro/page.tsx`, `src/app/pro/demande/page.tsx`, `src/features/pro/actions.ts`

- [ ] **Step 1:** `pro/page.tsx` : landing B2B (avantages : tarifs pro, commande en ligne, interlocuteur dédié), CTA « Faire une demande ». **Step 2:** `pro/demande/page.tsx` : si non connecté → invite à créer un compte ; sinon formulaire (company_name, siret, phone, message). **Step 3: Server Action** `submitProApplication` (`'use server'`) : insert `pro_applications` (RLS `with check auth.uid()=user_id`), statut `pending`, + email de notification à Alexandre (Resend). **Step 4:** Test schéma Zod (company_name requis). **Step 5:** commit `feat(pro): landing + application form + admin notification email`.

---

### Task 7 : Application des prix PRO partout

**Files:**
- Modify: `src/features/catalog/queries.ts`, `src/components/shop/ProductCard.tsx`, `src/components/shop/VariantPicker.tsx`, `src/features/cart/store.ts`, `src/features/checkout/createOrder.ts`
- Create: `src/features/pro/pricing.ts`
- Test: `tests/unit/pro-pricing.test.ts`

- [ ] **Step 1: Test (rouge)**

Create `tests/unit/pro-pricing.test.ts`:
```typescript
import { describe, it, expect } from 'vitest'
import { displayPriceCents } from '@/features/pro/pricing'

describe('displayPriceCents', () => {
  it('renvoie le prix public pour un non-pro', () => {
    expect(displayPriceCents(10000, { isPro:false, proPercent:20 })).toBe(10000)
  })
  it('applique la remise pour un pro', () => {
    expect(displayPriceCents(10000, { isPro:true, proPercent:20 })).toBe(8000)
  })
})
```

- [ ] **Step 2: Implémenter**

Create `src/features/pro/pricing.ts`:
```typescript
import { applyProDiscount } from '@/lib/money'
export function displayPriceCents(publicCents: number, ctx: { isPro: boolean; proPercent: number }): number {
  return ctx.isPro ? applyProDiscount(publicCents, ctx.proPercent) : publicCents
}
```

- [ ] **Step 3:** test vert.
- [ ] **Step 4: Propager le contexte pro** : un provider serveur expose `{ isPro, proPercent }` (depuis `getCurrentUser` + `getSettings`) ; `ProductCard`/`VariantPicker` affichent `displayPriceCents(...)` avec, si pro, mention « prix pro » + prix public barré. **Step 5:** `createOrder` lit le rôle serveur → `is_pro_order=true` + `computeTotals(... isPro:true ...)` (déjà supporté Plan 3). **Step 6:** Vérif manuelle : un compte pro voit les prix remisés sur catalogue/fiche/panier/checkout ; un invité voit le prix public. **Step 7:** `npm test && npm run build` verts. **Step 8:** commit `feat(pro): apply pro pricing across catalog, cart, checkout`.

---

## Self-Review (Plan 4)

- **Couverture §3 / §4.2** : auth client ✓, compte optionnel ✓, demande PRO en ligne ✓, rôle pro ✓, prix pro dérivé partout ✓, compte obligatoire pour PRO ✓.
- **Validation par Alexandre** : insert de la demande ici ; l'écran admin de validation est au **Plan 5** (passage du rôle à `pro`).
- **Cohérence types** : `displayPriceCents` s'appuie sur `applyProDiscount` (Plan 1) ; `computeTotals` (Plan 3) inchangé.

**Sortie de Plan 4 :** comptes + B2B fonctionnels (sauf écran de validation admin). ➡️ Plan 5.
