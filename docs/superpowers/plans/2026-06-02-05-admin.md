# Plan 5 — Admin back-office — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development ou superpowers:executing-plans.
> **Pré-requis :** Plans 1-4 terminés. Lire `00-conventions.md`.

**Goal:** Donner à Alexandre (`role='admin'`) un back-office pour gérer produits/variantes/stock/images, traiter les commandes (statuts), valider les demandes PRO et régler les paramètres (remise pro %, frais de port, infos magasin).

**Architecture:** Routes `/admin/*` protégées par `requireRole('admin')`. Écritures via Server Actions utilisant le client **service role** (contournant la RLS de façon contrôlée, après vérification du rôle côté serveur). Upload images vers Supabase Storage (bucket `product-images`).

**Tech Stack:** Next.js Server Actions, Supabase Storage, shadcn (Table, Dialog, Form, Select).

---

### Task 1 : Policies admin + bucket Storage + garde d'accès

**Files:**
- Create: `supabase/migrations/0007_admin_rls.sql`, `src/app/admin/layout.tsx`

- [ ] **Step 1: Policies admin + helper SQL**

Create `supabase/migrations/0007_admin_rls.sql`:
```sql
-- Helper : l'utilisateur courant est-il admin ?
create function is_admin() returns boolean language sql stable security definer as $$
  select exists (select 1 from profiles where id = auth.uid() and role = 'admin');
$$;
-- Écriture admin sur le catalogue
create policy "admin writes products" on products for all using (is_admin()) with check (is_admin());
create policy "admin writes variants" on product_variants for all using (is_admin()) with check (is_admin());
create policy "admin writes categories" on categories for all using (is_admin()) with check (is_admin());
create policy "admin writes brands" on brands for all using (is_admin()) with check (is_admin());
create policy "admin writes objectives" on objectives for all using (is_admin()) with check (is_admin());
create policy "admin writes product_objectives" on product_objectives for all using (is_admin()) with check (is_admin());
-- Commandes
create policy "admin reads all orders" on orders for select using (is_admin());
create policy "admin updates orders" on orders for update using (is_admin());
create policy "admin reads all order_items" on order_items for select using (is_admin());
-- PRO
create policy "admin reads applications" on pro_applications for select using (is_admin());
create policy "admin updates applications" on pro_applications for update using (is_admin());
create policy "admin reads profiles" on profiles for select using (is_admin());
create policy "admin updates profiles" on profiles for update using (is_admin());
-- Settings
create policy "admin updates settings" on settings for update using (is_admin());
-- Storage bucket pour les images produits
insert into storage.buckets (id, name, public) values ('product-images','product-images', true) on conflict do nothing;
create policy "public read product images" on storage.objects for select using (bucket_id = 'product-images');
create policy "admin manages product images" on storage.objects for all
  using (bucket_id = 'product-images' and is_admin()) with check (bucket_id = 'product-images' and is_admin());
```

- [ ] **Step 2:** Appliquer, `get_advisors` security OK.
- [ ] **Step 3:** `admin/layout.tsx` : `await requireRole('admin')` + nav latérale (Dashboard, Produits, Commandes, Demandes PRO, Réglages). **Step 4:** Vérif manuelle : un non-admin sur `/admin` est redirigé. **Step 5:** commit `feat(admin): admin RLS + storage bucket + protected layout`.

> ⚠️ **Action humaine** : promouvoir le compte d'Alexandre en admin une fois (`update profiles set role='admin' where email='...';` via MCP `execute_sql`).

---

### Task 2 : Dashboard

**Files:**
- Create: `src/app/admin/page.tsx`, `src/features/admin/stats.ts`

- [ ] **Step 1:** `stats.ts` : `getDashboardStats()` (CA payé du mois, nb commandes à préparer, nb variantes en stock bas `< 5`, demandes PRO en attente). **Step 2:** `admin/page.tsx` : cartes de stats + liste 5 dernières commandes + alertes stock bas. **Step 3:** Test transform des stats. **Step 4:** commit `feat(admin): dashboard with KPIs + low-stock alerts`.

---

### Task 3 : Gestion produits (liste + création/édition)

**Files:**
- Create: `src/app/admin/produits/page.tsx`, `src/app/admin/produits/[id]/page.tsx`, `src/features/admin/products.ts`, `src/components/admin/ProductForm.tsx`
- Test: `tests/unit/product-form-schema.test.ts`

- [ ] **Step 1: Schéma produit (Zod)** : name, slug (auto depuis name si vide), description, brand_id, category_id, objectives[], is_active. **Step 2:** test (slug auto-généré, name requis). **Step 3:** test vert.
- [ ] **Step 4: Server Actions** `products.ts` : `createProduct`, `updateProduct`, `toggleActive`, `deleteProduct` (service role après `requireRole('admin')`). Prix saisis en € → `eurosToCents`.
- [ ] **Step 5:** `produits/page.tsx` : table (nom, marque, prix mini, stock total, actif) + recherche + bouton « Nouveau ». **Step 6:** `ProductForm` (shadcn Form) pour créer/éditer. **Step 7:** Vérif manuelle : créer un produit le fait apparaître dans `/boutique`. **Step 8:** commit `feat(admin): product CRUD`.

---

### Task 4 : Gestion des variantes + stock + images

**Files:**
- Create: `src/components/admin/VariantManager.tsx`, `src/components/admin/ImageUploader.tsx`, `src/features/admin/variants.ts`, `src/features/admin/upload.ts`

- [ ] **Step 1:** `variants.ts` : `addVariant`, `updateVariant` (label, sku, price en €→cents, stock_qty), `deleteVariant`, `setStock(variantId, qty)`. **Step 2:** `VariantManager` intégré à la fiche produit admin : table éditable des variantes + ajustement rapide du stock. **Step 3:** `upload.ts` + `ImageUploader` : upload vers le bucket `product-images`, renvoie l'URL publique, ajoute à `products.images[]`. **Step 4:** Test transform (€ saisi → cents stockés). **Step 5:** Vérif manuelle : modifier le stock se reflète sur la fiche produit (badge rupture si 0). **Step 6:** commit `feat(admin): variant manager + stock + image upload`.

---

### Task 5 : Gestion des commandes

**Files:**
- Create: `src/app/admin/commandes/page.tsx`, `src/app/admin/commandes/[id]/page.tsx`, `src/features/admin/orders.ts`

- [ ] **Step 1:** `orders.ts` : `listOrders(filters)` (statut, mode), `getOrder(id)` (avec items), `updateOrderStatus(id, status)`, `markPickupPaid(id)` (passe `to_pay_pickup` → `paid`). **Step 2:** `commandes/page.tsx` : table (date, client/email, total, mode, statut) + filtres. **Step 3:** `commandes/[id]/page.tsx` : détail + items + changement de statut (Select) + bouton « Marquer payé » (retrait). **Step 4:** Test de la machine à états : transitions autorisées (`isValidTransition(from, to)`), ex. `to_pay_pickup → paid → preparing → ready → picked_up`. **Step 5:** test vert. **Step 6:** commit `feat(admin): order management + status workflow`.

---

### Task 6 : Validation des demandes PRO

**Files:**
- Create: `src/app/admin/demandes-pro/page.tsx`, `src/features/admin/proApplications.ts`

- [ ] **Step 1:** `proApplications.ts` : `listApplications(status)`, `approveApplication(id)` (transaction : `pro_applications.status='approved'`, `reviewed_at=now()`, `profiles.role='pro'` pour le `user_id`), `rejectApplication(id)`. **Step 2:** `demandes-pro/page.tsx` : table des demandes `pending` (entreprise, SIRET, contact) + boutons Approuver/Refuser. **Step 3:** Email au partenaire à l'approbation (Resend). **Step 4:** Test : `approveApplication` met bien le rôle à `pro` (mock service role). **Step 5:** Vérif manuelle : approuver une demande → le compte voit les prix pro. **Step 6:** commit `feat(admin): pro application review (approve/reject -> role)`.

---

### Task 7 : Réglages

**Files:**
- Create: `src/app/admin/reglages/page.tsx`, `src/features/admin/settings.ts` (compléter avec `updateSettings`)

- [ ] **Step 1:** `updateSettings` (Server Action, admin) : `pro_discount_percent`, `free_shipping_threshold` (€→cents), `shipping_fee` (€→cents), infos magasin, `whatsapp_number`. **Step 2:** `reglages/page.tsx` : formulaire. **Step 3:** Test schéma (remise 0-90, montants ≥ 0). **Step 4:** Vérif manuelle : changer la remise pro % se reflète immédiatement sur les prix pro. **Step 5:** commit `feat(admin): store settings (pro discount, shipping, store info)`.

---

## Self-Review (Plan 5)

- **Couverture §4.3** : dashboard ✓, produits/variantes/stock/images ✓, commandes + statuts + marquer payé ✓, validation PRO → rôle ✓, réglages (remise %, port, infos) ✓.
- **Sécurité** : toutes les écritures passent par `requireRole('admin')` côté serveur **et** RLS `is_admin()` en défense en profondeur ✓.
- **Cohérence types** : statuts commande = enums de `00-conventions.md` ; prix toujours € en saisie → cents en stockage (`eurosToCents`).

**Sortie de Plan 5 :** Alexandre gère tout sans aide technique. ➡️ Plan 6.
